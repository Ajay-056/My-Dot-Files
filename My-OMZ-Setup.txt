Debian Based
-------------
sudo apt update && sudo apt upgrade
sudo apt install zsh

Arch Based
----------
sudo pacman -Syu
sudo pacman -Syy zsh

Next Steps
----------
chsh -s $(which zsh)
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
git clone https://github.com/romkatv/powerlevel10k $ZSH_CUSTOM/themes/powerlevel10k
git clone https://github.com/zsh-users/zsh-autosuggestions $ZSH_CUSTOM/plugins/zsh-autosuggestions
git clone https://github.com/zsh-users/zsh-syntax-highlighting $ZSH_CUSTOM/plugins/zsh-syntax-highlighting

Add Following Lines in .zshrc File
----------------------------------
ZSH_THEME="powerlevel10k/powerlevel10k"
POWERLEVEL9K_MODE="Any Nerd Font" // Use Nerd fonts to get exepected results (My choice: Caskadia Code (MS))

// URL:  https://github.com/microsoft/cascadia-code

Following are the Plugins Im using personally (Add / Remove the pluggins as per the need)
---------------------------------------------------------
// URL:  https://github.com/ohmyzsh/ohmyzsh/wiki/Plugins

plugins=(
	git
	z
	aliases
#	globalias
	aws
#	rust
	catimg
	colored-man-pages
	copypath
	copyfile
#	dircycle
	dirhistory
	docker
	docker-compose
	encode64
	genpass
	perms
	systemadmin
	systemd
	transfer
	vscode
	pip
	git-extras
	git-prompt
	jsontools
	zsh-autosuggestions
	zsh-syntax-highlighting
	command-not-found
	dotenv
#	macos
	rake
	last-working-dir
	web-search
	extract
	history
	sudo
	safe-paste
	npm
	nvm
	node
	magic-enter
#	ubuntu
# 	adb
 	ansible
	ant
	archlinux
	autopep8
	battery
	bgnotify
	branch
	bundler
	colorize
	copybuffer
	dirpersist
	emoji-clock
	fancy-ctrl-z
	frontend-search
#	gatsby
	gcloud
#	gem
	gh
	git-auto-fetch
	git-escape-maic
	git-flow
	github
	gitignore
	golang
	helm
	hitchhiker
	istioctl
	jenv
#	jira
 	kops
	kubectl
	kubectx
	man
	microk8s
	minikube
	nmap
	pipenv
	pyenv
	pylint
	python
#	rails
	ripgrep
	screen
	terraform
	ufw
	urltools
)

